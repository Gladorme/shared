{
  "extends": "../tsconfig.base.json",
  // Includes JSON just for test compilation/test files
  "include": ["src", "src/**/*.json"],
  "compilerOptions": {
    "paths": {
      "@perses-dev/plugin-system": ["./src"]
    }
  }
}
